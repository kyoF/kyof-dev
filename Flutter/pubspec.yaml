name: hello_world_from_flutter
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.16.2 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.0.5
  url_launcher: ^6.0.3


  cupertino_icons: ^1.0.2
  google_maps_flutter: ^2.1.3
  google_maps_flutter_web: ^0.3.2+1
  flutter_dotenv: ^5.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_driver:
    sdk: flutter

  test: any

  flutter_lints: ^1.0.0

  flutter_launcher_icons: ^0.9.0


flutter_icons:
  android: true
  ios: true
  image_path: "assets/icon/icon.png"

flutter:

  uses-material-design: true

assets:
  - .env